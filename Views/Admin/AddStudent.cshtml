@model IITS_Programming_Club.Models.StudentInfo
@using Microsoft.AspNetCore.Http;
@using IITS_Programming_Club.Models;
    <div class="container">
        @{
            string s = Context.Session.GetString("adminid");
            if (s == null)
            {
                @:Please login First
            }
            else
            {
                <h4>StudentInfo</h4>
                <hr />
                @ViewBag.message
                <div class="row">
                    <div class="col-md-4">
                        <form asp-action="AddStudent">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <div class="form-group">
                                <label asp-for="student_id" class="control-label"></label>
                                <input asp-for="student_id" class="form-control" />
                                <span asp-validation-for="student_id" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="student_pass" class="control-label"></label>
                                <input asp-for="student_pass" class="form-control" />
                                <span asp-validation-for="student_pass" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="student_division" class="control-label"></label>
                                <select asp-for="student_division">
                                    <option selected disabled>--- select option ---</option>
                                    <option>Adcance</option>
                                    <option>Intermediate</option>
                                    <option>Beginner</option>
                                </select>
                                <span asp-validation-for="student_division" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="student_status" class="control-label"></label>
                                <select asp-for="student_status">
                                    <option selected disabled>--- select option ---</option>
                                    <option>Active</option>
                                    <option>Block</option>
                                </select>
                                <span asp-validation-for="student_status" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <input type="submit" value="Save" class="btn btn-primary" />
                            </div>
                        </form>
                    </div>
                </div>

                <div>
                    <a asp-action="AdminWork">Admin Work Panel</a>
                </div>

            }
        }
    </div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
